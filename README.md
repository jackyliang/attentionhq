# AttentionHQ

Linear-style Kanban for managing concurrent Devin sessions and GitHub issues.

Open `index.html` in a browser — static mockup, no build step yet.

See `plan/` for the discovery brief, build spec, and realtime-updates API request.
